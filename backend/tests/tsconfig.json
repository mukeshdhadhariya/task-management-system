{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "rootDir": "..",
    "types": ["node", "jest"]
  },
  "include": ["../src/**/*.ts", "../src/**/*.d.ts", "./**/*.ts", "../jest.setup.ts"],
  "exclude": ["../dist", "../node_modules", "../prisma", "../prisma.config.ts"]
}
